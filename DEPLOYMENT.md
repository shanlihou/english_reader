# 🚀 自动化构建与部署指南

## GitHub Actions 自动构建 APK

本项目使用 GitHub Actions 实现自动化构建，当推送版本标签时自动编译并发布 APK。

### 📋 构建流程

当推送版本标签（如 `v1.0.0`）时，GitHub Actions 将自动执行以下操作：

1. ✅ **环境准备**
   - 检出代码
   - 安装 JDK 11
   - 设置 Flutter 3.8.0

2. 🔧 **编译应用**
   - 获取依赖包
   - 构建 Debug APK
   - 构建 Release APK

3. 📦 **发布产物**
   - 上传 APK 作为构建产物
   - 创建 GitHub Release
   - 自动生成发布说明

### 🏷️ 版本标签格式

使用语义化版本号格式：

```bash
# 创建版本标签
git tag v1.0.0
git push origin v1.0.0

# 其他示例
git tag v1.1.0
git tag v2.0.0
git tag v1.0.1
```

### 📱 APK 类型

GitHub Actions 将生成两种 APK：

1. **Debug APK** (`app-debug.apk`)
   - 包含调试信息
   - 文件较大
   - 适用于测试和开发

2. **Release APK** (`app-release.apk`)
   - 已优化和签名
   - 文件较小
   - 适用于生产环境

### 📊 构建状态

查看构建状态：
- **Actions 页面**：https://github.com/USERNAME/english_reader/actions
- **Releases 页面**：https://github.com/USERNAME/english_reader/releases

### 📥 下载 APK

1. 进入项目 Releases 页面
2. 选择对应版本
3. 下载 `app-release-vX.Y.Z.apk` 或 `app-debug-vX.Y.Z.apk`

### 🔄 手动构建（本地）

也可以在本地构建 APK：

```bash
# Debug APK
flutter build apk --debug

# Release APK
flutter build apk --release

# 查看输出位置
ls build/app/outputs/flutter-apk/
```

### ⚙️ 工作流配置

工作流文件位置：`.github/workflows/build-apk.yml`

主要配置：
- **触发条件**：`tags: - 'v*.*.*'`
- **运行环境**：`ubuntu-latest`
- **Flutter 版本**：`3.8.0`

### 🎯 发布步骤

完整发布流程：

```bash
# 1. 确保所有更改已提交
git status
git add .
git commit -m "feat: 发布 v1.0.0 版本"

# 2. 创建版本标签
git tag v1.0.0

# 3. 推送标签（触发自动构建）
git push origin v1.0.0

# 4. 等待构建完成（约5-10分钟）
# 在 Actions 页面查看进度

# 5. 构建完成后，在 Releases 页面下载 APK
```

### 📝 自动生成的 Release 说明

每次发布会自动生成包含以下内容的 Release：

```
## 🎉 英语阅读器 v1.0.0

### 📱 下载链接

- **调试版 (Debug APK)**
- **正式版 (Release APK)**

### ✨ 功能特性

- 📖 本地文本阅读
- 🔍 字体大小调整
- 👆 手势翻页
- 🌍 在线翻译
- 📚 历史记录
- ⭐ 单词本
- 📊 阅读进度
- 🌙 深色/浅色主题

### 💡 安装说明

1. 下载对应版本的APK文件
2. 在设备上安装（需要开启"未知来源"权限）
3. 开始使用英语阅读器！

---
由 GitHub Actions 自动构建 🚀
```

### 🐛 故障排除

**构建失败？**

1. 检查 Actions 日志
2. 常见错误：
   - Flutter 版本不兼容
   - 依赖冲突
   - 代码格式问题

**解决方案：**

```bash
# 修复代码格式
flutter format .

# 分析代码问题
flutter analyze

# 运行测试
flutter test
```

### 📚 参考资料

- [Flutter 构建 APK 指南](https://docs.flutter.dev/deployment/android)
- [GitHub Actions 文档](https://docs.github.com/en/actions)
- [语义化版本](https://semver.org/lang/zh-CN/)

---

## 🎉 Happy Building! 🎉
